// Internal atom — intentionally NOT re-exported from `atoms/index.ts` (mirrors
// `atoms/NodeIdentityLabel`). Import by path where needed.
export { EditableText } from './EditableText';
export type { EditableTextProps, EditableTextHandle } from './EditableText';
