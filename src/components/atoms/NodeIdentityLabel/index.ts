// Internal primitive — intentionally NOT re-exported from the public atoms
// barrel (`src/components/atoms/index.ts`). Import directly via
// `@/components/atoms/NodeIdentityLabel` inside the library only.
export { NodeIdentityLabel } from './NodeIdentityLabel';
export type { NodeIdentityLabelProps } from './NodeIdentityLabel';
